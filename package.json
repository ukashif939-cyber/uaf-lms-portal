{
  "name": "uaf-lms-portal",
  "private": true,
  "scripts": {
    "prepare:firebase": "node scripts/prepare-firebase-deploy.js",
    "build:frontend": "node scripts/build-frontend-firebase.js",
    "deploy": "node scripts/prepare-firebase-deploy.js && node scripts/build-frontend-firebase.js && cd functions && npm install && cd .. && npx firebase-tools deploy --project uaf-lms-main",
    "deploy:hosting": "npm run build:frontend && npx firebase-tools deploy --project uaf-lms-main --only hosting"
  }
}
